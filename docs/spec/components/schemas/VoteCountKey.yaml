type: object
required:
  - id
  - type
properties:
  id:
    type: string
    pattern: "^[0-9]+$"
    example: "333"
    description: ID of the voting to which the vote
  type:
    type: string
    enum: 
      - "vote_count"